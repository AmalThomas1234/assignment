export interface Transaction {
  id: string;
  date: string;
  amount: number;
  description: string;
  employeeId: string;
  employeeName: string;
  // Add other fields as necessary based on your data structure
}